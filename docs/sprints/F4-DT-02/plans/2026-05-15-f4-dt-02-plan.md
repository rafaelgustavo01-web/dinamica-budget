# Plano — F4-DT-02 QA Residual Debt Cleanup

## Entregas
1. Match PQ: GET /match/status deve retornar estado derivado de pq_itens quando _match_tasks estiver vazio.
2. UI: remover caracteres U+FFFD/mojibake da tela ProposalItemsExpandedPage.
3. API: trocar dict solto por schemas Pydantic nos payloads de itens da proposta e itens BCU.
4. Docs: atualizar backlog e gerar review/walkthrough.

## Gates
- git diff --check
- grep por U+FFFD em app/frontend/src deve retornar vazio.
- Backend: compileall + pytest focado em pq_match_review/proposta_service.
- Frontend: npm run lint, npm test -- --run, npm run build.

## Critério de aceite
A sprint só pode ir para TESTED com gates verdes e commit/push em main.
