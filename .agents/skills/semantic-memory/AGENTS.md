# AGENTS.md — Semantic Memory Skill
# Per-agent configuration. Local file — never transmitted to Supabase.
# Each block: agent slug must match agent_name used in API calls (lowercase).
#
# elevation_password: blank = not yet set (skill will prompt on first /super_mem use)

## agent: claude
elevation_password:

## agent: gpt4
elevation_password:

## agent: orchestrator
elevation_password:
